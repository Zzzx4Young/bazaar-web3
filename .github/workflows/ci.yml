name: ci

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json
      - name: Install
        run: npm ci
      - name: Typecheck
        run: npm run typecheck
      - name: Lint
        run: npm run lint
      - name: Unit tests (Vitest)
        run: npm run test
      - name: Build
        run: npm run build
        env:
          NEXT_TELEMETRY_DISABLED: '1'

  backend:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    defaults:
      run:
        working-directory: backend
    env:
      PRISMA_HIDE_UPDATE_MESSAGE: '1'
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22.23.1'
          cache: 'npm'
          cache-dependency-path: backend/package-lock.json
      - name: Pin npm
        run: npm install --global npm@10.9.8
      - name: Install locked dependencies
        run: npm ci
      - name: Generate and build
        run: npm run build
      - name: Typecheck
        run: npm run typecheck
      - name: Lint
        run: npm run lint
      - name: Unit tests
        run: node --test tests/*.test.mjs
      - name: Start isolated PostgreSQL
        working-directory: .
        run: |
          bash infra/scripts/init-secrets.sh
          docker compose -f infra/compose.yaml --profile test up -d --wait --wait-timeout 120 postgres-test
      - name: Database integration tests
        run: node scripts/with-test-db.mjs
      - name: Database service status on failure
        if: failure()
        working-directory: .
        run: docker compose -f infra/compose.yaml --profile test ps -a
      - name: Stop test database
        if: always()
        working-directory: .
        run: docker compose -f infra/compose.yaml --profile test stop postgres-test
